<h1>編集画面</h1>

<%= form_with model: @park do |f| %>

　<h4>画像</h4>
  <%= f.file_field :image %>

  <h4>公園名</h4>
  <%= f.text_field :name%>

  <h4>説明文</h4>
  <%= f.text_area :description %>

  <%= f.submit '保存' %>

<% end %>
<%= link_to '戻る', park_path %>